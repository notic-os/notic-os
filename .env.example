# Server
PORT=3000
BASE_URL=http://localhost:3000
SESSION_SECRET=change-me
SESSION_COOKIE_SECURE=false

# Admin auth (generate bcrypt hash with: node -e "require('bcryptjs').hash(process.argv[1],10).then(console.log)" yourPassword)
ADMIN_USER=admin
ADMIN_PASS_HASH=
SLA_HOURS=24

# Storage
TICKET_BACKEND=fs
DB_FILE=./data/tickets.db

# Email (SMTP – default)
USE_GRAPH=false
TO_EMAIL=support@example.com
FROM_EMAIL=support@example.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=smtp-user
SMTP_PASS=smtp-pass

# Email (Microsoft Graph – optional)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
GRAPH_SENDER_UPN=
HELPDESK_EMAIL=
